# This repo uses env files per package.
#
# Copy and fill:
# - apps/web/.env.example -> apps/web/.env.local
# - packages/db/.env.example -> packages/db/.env
#
# Legacy fallback:
# scripts/setup.ts still reads this root file if present, but new setups
# should use package env files.
